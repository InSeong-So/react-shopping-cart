import { ProductItem } from '@/components';
import { ProductContainer } from '@/template/Product';

const ProductDetailPage = () => (
  <ProductContainer.Detail>
    <ProductItem type="detail" />
  </ProductContainer.Detail>
);

export default ProductDetailPage;
